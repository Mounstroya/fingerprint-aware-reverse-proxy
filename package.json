{
  "name": "reverse-proxy-portfolio",
  "version": "2.0.0",
  "description": "curl-based reverse proxy demonstrating WAF/TLS-fingerprint-aware proxying, per-session cookie jars, and a generic third-party token capture + relay pattern.",
  "main": "src/server.js",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "mock": "node mock/server.js",
    "demo": "bash scripts/demo.sh",
    "lint": "eslint .",
    "test": "node --test test/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.1",
    "p-limit": "^3.1.0",
    "pino": "^9.5.0",
    "pino-http": "^10.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "nodemon": "^3.1.0"
  }
}
